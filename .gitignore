# Dependencies
/node_modules

# Playwright browser data
/playwright_profile

# Local environment variables
.env

# Job-specific folders
/uploads
/outputs

# Google OAuth credentials (keep off git)
/credentials.json
/tokens.json

# Pipeline API keys (never commit — use pipeline-config.example.json as template)
/pipeline-config.json

# CSV data files
*.csv
