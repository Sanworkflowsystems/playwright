# Ignore node_modules, as they will be installed inside the container
node_modules

# Ignore local Playwright browser data, as the Docker image has its own
playwright_profile

# Ignore job-specific folders
uploads
outputs

# Ignore git folder
.git
